% figure properties for PLoS Biology paper

format = 'png'; % for panels tricky for EPS (e.g. Pcolor plots)
color = 'rgb';
dpi = 1200;
fontsize = 7;
plotlinewidth = 0.75;
errorlinewidth = 0.5;
axlinewidth = 0.5;
% M = 2;  % marker size for filled scatter plots
% M_uni = 3; % marker size for univariate scatter plots

Units = 'centimeters';

% colors for learning, rule change and other (non-learning) sessions
Clearn = 'r'; % color for learning sessions
Crulec = [.3 .3 .3]; % color for rule change sessions
Cother = 'b'; % color for other sessions

% % colors for comparing session types
% dot.learning = [0 0 0];
% plotline.learning = [0.7 0.7 0.7];  % for links and error bars
% dot.rule = [0.2 0.2 0.8];
% plotline.rule = [0.7 0.7 0.7];  % for links and error bars
% dot.stable = [0.8 0.2 0.2];
% plotline.stable = [0.7 0.7 0.7];  % for links and error bars
% dot.rest = [0.7 0.4 0.7];
% plotline.rest = [0.7 0.7 0.7];  % for links and error bars

